export type MenuPoint = {
    id: string;
    link: string;
    name: string;
};

export type FreeSlots = {
    id: number;
    value: string;
    label: string;
};

export type ScheduleDay = {
    start: number;
    end: number;
};
