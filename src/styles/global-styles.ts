import { css } from '@emotion/react';

export const globalStyles = css`
    html,
    body {
        padding: 0;
        margin: 0;
        min-width: 320px;
        overflow-x: hidden;
        overflow-y: auto;
    }
`;
